% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processes.R
\name{process_bm_drift_poisson}
\alias{process_bm_drift_poisson}
\title{Create a drifted Brownian motion plus Poisson process}
\usage{
process_bm_drift_poisson(mu = 0, sigma = 1, poisson_intensity = 1)
}
\arguments{
\item{mu}{Drift coefficient. Defaults to 0.}

\item{sigma}{BM volatility. Defaults to 1.}

\item{poisson_intensity}{Poisson intensity (non-negative). Defaults to 1.
Set to 0 for pure drifted BM without jumps.}
}
\value{
A function with signature \verb{function(x, dt)} that takes a
numeric vector of current positions and returns the updated positions
after one step. Has class \code{"iftp_process"}. When
\code{poisson_intensity > 0}, the return value carries an attribute
\code{"jump_size"} (numeric vector): the Poisson jump component of each
particle's increment. Used by particle system loops to create segment
breaks at jump discontinuities.
}
\description{
Returns a transition kernel for the sum of a (possibly drifted) Brownian
motion and an independent Poisson process. When \code{poisson_intensity = 0},
reduces to a drifted Brownian motion without jumps.
}
\examples{
proc <- process_bm_drift_poisson(mu = 0.5)
set.seed(42)
x_new <- proc(rep(0, 1000), 0.01)

# Without jumps (pure drifted BM)
proc_no_jump <- process_bm_drift_poisson(mu = 0.5, poisson_intensity = 0)

}
\seealso{
Other processes: 
\code{\link{process_bessel}()},
\code{\link{process_bm}()},
\code{\link{process_cauchy}()},
\code{\link{process_cev}()},
\code{\link{process_cir}()},
\code{\link{process_negative_gamma}()},
\code{\link{process_ou}()},
\code{\link{process_wright_fisher}()}
}
\concept{processes}
