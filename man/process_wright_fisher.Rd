% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processes.R
\name{process_wright_fisher}
\alias{process_wright_fisher}
\title{Create a neutral Wright-Fisher diffusion}
\usage{
process_wright_fisher(theta1 = 1, theta2 = 1, lower = 0, upper = 1)
}
\arguments{
\item{theta1}{Mutation rate toward the upper boundary (positive number).
Defaults to 1.}

\item{theta2}{Mutation rate toward the lower boundary (positive number).
Defaults to 1.}

\item{lower}{Lower boundary of the interval. Defaults to 0.}

\item{upper}{Upper boundary of the interval. Defaults to 1.}
}
\value{
A function with signature \verb{function(x, dt)} that takes a
numeric vector of current positions and returns the updated positions
after one step. Has class \code{"iftp_process"}.
}
\description{
Returns a transition kernel for the neutral Wright-Fisher diffusion
on the interval \eqn{[a, b]}{[a, b]}, satisfying the SDE
\eqn{\mathrm{d}X_t = \tfrac{1}{2}[\theta_1(b - X_t) -
\theta_2(X_t - a)]\,\mathrm{d}t +
\sqrt{(X_t - a)(b - X_t)}\,\mathrm{d}W_t}{dX =
0.5*[theta1*(b - X) - theta2*(X - a)]*dt +
sqrt((X - a)*(b - X))*dW}.
The drift is determined by mutation rates \eqn{\theta_1}{theta1}
and \eqn{\theta_2}{theta2}; "neutral" refers to the absence of
natural selection. The volatility vanishes at both boundaries.
Discretised via the Euler-Maruyama scheme with clamping to
\eqn{[a, b]}{[a, b]}.
}
\examples{
wf <- process_wright_fisher(theta1 = 1, theta2 = 1)
set.seed(42)
x <- rep(0.5, 1000)
for (i in seq_len(100)) x <- wf(x, 0.01)
all(x >= 0 & x <= 1)

}
\references{
Jenkins, P.A. and \enc{Spanò}{Spano}, D. (2017). Exact simulation of the
Wright--Fisher diffusion. \emph{Ann. Appl. Probab.}, 27(3), 1478--1509.
\doi{10.1214/16-AAP1236}
}
\seealso{
Other processes: 
\code{\link{process_bessel}()},
\code{\link{process_bm}()},
\code{\link{process_bm_drift_poisson}()},
\code{\link{process_cauchy}()},
\code{\link{process_cev}()},
\code{\link{process_cir}()},
\code{\link{process_negative_gamma}()},
\code{\link{process_ou}()}
}
\concept{processes}
