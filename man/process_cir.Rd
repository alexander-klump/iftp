% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processes.R
\name{process_cir}
\alias{process_cir}
\title{Create a Cox-Ingersoll-Ross process}
\usage{
process_cir(kappa = 1, theta = 1, sigma = 1)
}
\arguments{
\item{kappa}{Mean-reversion speed (positive number). Defaults to 1.}

\item{theta}{Long-term mean (positive number). Defaults to 1.}

\item{sigma}{Volatility (positive number). Defaults to 1.}
}
\value{
A function with signature \verb{function(x, dt)} that takes a
numeric vector of current positions and returns the updated positions
after one step. Has class \code{"iftp_process"}.
}
\description{
Returns a transition kernel for the Cox-Ingersoll-Ross (CIR) diffusion
\eqn{\mathrm{d}X_t = \kappa(\theta - X_t)\,\mathrm{d}t +
\sigma\sqrt{X_t}\,\mathrm{d}W_t}{dX = kappa*(theta-X)*dt +
sigma*sqrt(X)*dW}.
The process is mean-reverting toward \eqn{\theta}{theta} and takes
values in \eqn{[0, \infty)}{[0, Inf)}.
Discretised via the Euler-Maruyama scheme with reflection at 0.
}
\details{
The Feller condition \eqn{2\kappa\theta \ge \sigma^2}{2*kappa*theta >= sigma^2}
ensures the process stays strictly positive. When this condition is
violated, the process can reach zero; the Euler-Maruyama discretisation
uses reflection at 0 in both cases.
}
\examples{
cir <- process_cir(kappa = 2, theta = 1, sigma = 0.5)
set.seed(42)
x <- rep(1, 1000)
for (i in seq_len(100)) x <- cir(x, 0.01)
all(x >= 0)

}
\seealso{
Other processes: 
\code{\link{process_bessel}()},
\code{\link{process_bm}()},
\code{\link{process_bm_drift_poisson}()},
\code{\link{process_cauchy}()},
\code{\link{process_cev}()},
\code{\link{process_negative_gamma}()},
\code{\link{process_ou}()},
\code{\link{process_wright_fisher}()}
}
\concept{processes}
