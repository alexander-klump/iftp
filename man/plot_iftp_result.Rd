% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iftp_plot.R
\name{plot_iftp_result}
\alias{plot_iftp_result}
\alias{plot.iftp_result}
\title{Plot an IFTP boundary curve}
\usage{
plot_iftp_result(
  x,
  title = "IFTP Boundary Approximation",
  reference_line = NULL,
  cost_transform = TRUE
)

\method{plot}{iftp_result}(x, ...)
}
\arguments{
\item{x}{An \code{iftp_result} object from \code{\link[=iftp_anulova]{iftp_anulova()}},
\code{\link[=iftp_quantile]{iftp_quantile()}}, or \code{\link[=iftp_soft_killing]{iftp_soft_killing()}}.}

\item{title}{Plot title. Defaults to \code{"IFTP Boundary Approximation"}.}

\item{reference_line}{Optional numeric value to draw as a horizontal
reference line (e.g., the Shiryaev constant \code{pi / (2 * sqrt(2))}).}

\item{cost_transform}{Logical. If \code{TRUE} (default), boundary values are
transformed by the cost function before plotting, giving the boundary
in cost space. If \code{FALSE}, the raw particle positions at the survival
threshold are shown.}

\item{...}{Additional arguments passed from the S3 method (currently unused).}
}
\value{
A \code{ggplot} object (invisibly).
}
\description{
Plots a boundary approximation over time. Requires the
\pkg{ggplot2} package.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  b1 <- iftp_anulova(dist_exponential(),
    time_horizon = 2, time_steps = 50, n_particles = 500,
    seed = 42
  )
  plot_iftp_result(b1)
}

}
\seealso{
Other iftp: 
\code{\link{iftp_anulova}()},
\code{\link{iftp_quantile}()},
\code{\link{iftp_soft_killing}()}
}
\concept{iftp}
