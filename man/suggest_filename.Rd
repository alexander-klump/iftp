% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_export.R
\name{suggest_filename}
\alias{suggest_filename}
\title{Generate a descriptive filename for a simulation result}
\usage{
suggest_filename(result, ext = "csv", suffix = NULL)
}
\arguments{
\item{result}{An \code{iftp_result} or \code{ps_result} object.}

\item{ext}{File extension (without dot), e.g. \code{"csv"}, \code{"json"}, \code{"rds"}.}

\item{suffix}{Optional suffix appended before the timestamp (e.g.
\code{"matrix"} or \code{"segments"} for JSON export variants).}
}
\value{
A character string encoding all simulation parameters, e.g.
\code{"anulova_exponential_n100_t1_steps20_ou_s42_YYYYMMDD_HHMMSS.csv"}.
Includes process type (unless default BM), cost function (unless
default for the algorithm), seed (when stored in params),
path resolution, and grid method (when non-default).
}
\description{
Builds a filename from the simulation parameters stored in the result
object, appending a timestamp to ensure uniqueness.
}
\examples{
b <- iftp_anulova(dist_exponential(),
  time_horizon = 1, time_steps = 20,
  n_particles = 100, seed = 1
)
suggest_filename(b, "csv")
suggest_filename(b, "json")

r <- nbmp_simulate(n_particles = 10, time_horizon = 0.5, seed = 42)
suggest_filename(r, "csv")
suggest_filename(r, "json", suffix = "segments")

}
\seealso{
Other result-io: 
\code{\link{export_result}()},
\code{\link{load_result}()},
\code{\link{save_result}()}
}
\concept{result-io}
