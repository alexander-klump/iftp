% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_cache.R
\name{save_result}
\alias{save_result}
\title{Save a simulation result to disk}
\usage{
save_result(result, path, metadata = list())
}
\arguments{
\item{result}{An \code{iftp_result} or \code{ps_result} object.}

\item{path}{File path (should end in \code{.rds}).}

\item{metadata}{An optional named list of additional metadata to store
(e.g., description, algorithm parameters).}
}
\value{
The \code{path}, invisibly.
}
\description{
Saves an \code{iftp_result} or \code{ps_result} object as an \code{.rds} file with
metadata (timestamp, package version, and optional user-supplied fields).
}
\examples{
b <- iftp_anulova(dist_exponential(),
  time_horizon = 1, time_steps = 20,
  n_particles = 100, seed = 1
)
tmp <- tempfile(fileext = ".rds")
save_result(b, tmp, metadata = list(note = "test run"))
loaded <- load_result(tmp)
print(loaded)
file.remove(tmp)

}
\seealso{
Other result-io: 
\code{\link{export_result}()},
\code{\link{load_result}()},
\code{\link{suggest_filename}()}
}
\concept{result-io}
