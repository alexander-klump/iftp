% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_plot.R
\name{plot_ps_result}
\alias{plot_ps_result}
\alias{plot.ps_result}
\title{Plot particle system simulation results}
\usage{
plot_ps_result(
  x,
  reference_line = NULL,
  iftp_boundary = NULL,
  cost_transform = TRUE,
  title = NULL
)

\method{plot}{ps_result}(x, ...)
}
\arguments{
\item{x}{A \code{ps_result} object from \code{\link[=nbmp_simulate]{nbmp_simulate()}} or
\code{\link[=qtps_simulate]{qtps_simulate()}}.}

\item{reference_line}{Optional numeric value for a horizontal
reference line (e.g., Shiryaev constant \code{pi / (2 * sqrt(2))}).}

\item{iftp_boundary}{Optional data frame with \code{time} and \code{boundary}
columns or an \code{iftp_result} object for comparison overlay.}

\item{cost_transform}{Logical. If \code{TRUE} (default) and \code{iftp_boundary}
is an \code{iftp_result}, the cost function is applied to boundary values
before overlaying. If \code{FALSE}, raw positions are shown.}

\item{title}{Plot title. Defaults to an automatic title.}

\item{...}{Additional arguments passed from the S3 method (currently unused).}
}
\value{
A \code{ggplot} object (invisibly).
}
\description{
Visualizes particle paths from an N-BMP or QTPS simulation.
Requires the \pkg{ggplot2} package.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  result <- nbmp_simulate(n_particles = 20, time_horizon = 1, seed = 42)
  plot_ps_result(result)
}

}
\concept{ps}
