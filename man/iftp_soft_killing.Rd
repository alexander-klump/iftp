% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iftp_soft_killing.R
\name{iftp_soft_killing}
\alias{iftp_soft_killing}
\title{Soft-killing IFPT boundary approximation}
\usage{
iftp_soft_killing(
  cdf,
  time_horizon,
  killing_rate = 1,
  time_steps,
  n_particles,
  process = process_bm(),
  cost = function(x) -x,
  init = function(n) rep(0, n),
  capture_seed = FALSE,
  seed = NULL
)
}
\arguments{
\item{cdf}{Target CDF function or an \code{iftp_dist} object (created by
\code{dist_exponential()} etc.). Referred to as
\code{Fg} (hazard-related CDF) in the original paper.}

\item{time_horizon}{Time horizon (positive number).}

\item{killing_rate}{Soft killing rate. Defaults to 1.}

\item{time_steps}{Number of equidistant time steps.}

\item{n_particles}{Number of Monte Carlo particles.}

\item{process}{Transition kernel created by \code{\link[=process_bm]{process_bm()}} or a custom
function with signature \verb{function(x, dt)} that maps current
positions to new positions. Defaults to standard Brownian motion.}

\item{cost}{Cost function mapping particle positions to a selection
criterion. Equivalent to the negative fitness function \code{-F} from
Klump (2022, Section 2.4.1). Particles with \emph{high} \code{cost(x)}
are penalised (their weights are reduced). Defaults to
\code{function(x) -x}, which penalises the most-negative particles and
yields a \emph{lower} boundary. Use \code{identity} for an upper
boundary or \code{abs} for a two-sided (symmetric) boundary.}

\item{init}{Initial distribution function. A function taking a single
argument \code{n} and returning \code{n} starting positions. Defaults to
\code{function(n) rep(0, n)} (all particles at the origin).}

\item{capture_seed}{Logical. If \code{TRUE} and \code{seed} is \code{NULL}, a random
seed is generated, set, and stored in \code{result$params$seed} for
post-hoc reproducibility. Defaults to \code{FALSE}.}

\item{seed}{Optional integer seed for reproducibility. When provided,
it is always stored in \code{result$params$seed}.}
}
\value{
An \code{iftp_result} object containing:
\describe{
\item{boundary}{Data frame with \code{time} and \code{boundary} columns.
Boundary values are particle positions at the survival threshold.
The penalised region is
\code{\{(t, x) : cost(x) >= cost(b(t))\}}. \code{NA} at time 0.}
\item{params}{List of simulation parameters including \code{process},
\code{init}, and \code{cost}.}
}
}
\description{
Approximates the boundary of the inverse first-passage time problem using
the soft-killing approach from Klump & Kolb (2022). Particles are simulated
on an equidistant time grid and assigned weights that decrease through soft
killing, rather than hard removal.
}
\details{
Unlike the Anulova and quantile discretizations which remove particles
(hard killing), this method reduces particle weights when they exceed
the threshold in cost space (soft killing). The weighted quantile of
\code{cost(x)} determines the threshold at each step.

The time grid consists of \code{time_steps + 1} equidistant points with spacing
\code{time_horizon / time_steps}.

The algorithm:
\enumerate{
\item Initialise \code{n_particles} particles from \code{init} with equal weights.
\item At each time step, advance particles via the transition kernel.
\item Compute the conditional survival ratio
\verb{alpha = (1 - cdf(t_\{i+1\})) / (1 - cdf(t_i))}.
\item Find the \code{(1 - p)}-quantile of \code{cost(x)} (the survival quantile).
\item Reduce weights of particles where \code{cost(x) >= threshold} by factor
\code{exp(-killing_rate * dt)}.
}
}
\examples{
result <- iftp_soft_killing(
  cdf = dist_exponential(), time_horizon = 2,
  time_steps = 16, n_particles = 1000, seed = 42
)
head(result$boundary)

}
\references{
Klump, A. and Kolb, M. (2024). An elementary approach to the inverse
first-passage-time problem for soft-killed Brownian motion. \emph{Journal of
Applied Probability}, 61(1), 279--300. \doi{10.1017/jpr.2023.39}
}
\seealso{
Other iftp: 
\code{\link{iftp_anulova}()},
\code{\link{iftp_quantile}()},
\code{\link{plot_iftp_result}()}
}
\concept{iftp}
