% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processes.R
\name{process_cev}
\alias{process_cev}
\title{Create a constant elasticity of variance (CEV) process}
\usage{
process_cev(mu = 0, sigma = 1, gamma = 1)
}
\arguments{
\item{mu}{Drift coefficient (numeric). Defaults to 0.}

\item{sigma}{Volatility (positive number). Defaults to 1.}

\item{gamma}{Elasticity exponent (positive number). Defaults to 1.}
}
\value{
A function with signature \verb{function(x, dt)} that takes a
numeric vector of current positions and returns the updated positions
after one step. Has class \code{"iftp_process"}.
}
\description{
Returns a transition kernel for the CEV diffusion
\eqn{\mathrm{d}X_t = \mu X_t\,\mathrm{d}t +
\sigma X_t^\gamma\,\mathrm{d}W_t}{dX = mu*X*dt + sigma*X^gamma*dW}.
The process takes values in \eqn{(0, \infty)}{(0, Inf)}.
Discretised via the Euler-Maruyama scheme; positions are clamped to
a small positive value to maintain strict positivity.
}
\details{
The special case \eqn{\gamma = 1}{gamma = 1} gives geometric Brownian
motion (GBM), and \eqn{\gamma = 1/2}{gamma = 0.5} gives a CIR-like
square-root volatility without mean reversion.
}
\examples{
# Geometric Brownian motion (gamma = 1)
gbm <- process_cev(mu = 0.05, sigma = 0.2, gamma = 1)
set.seed(42)
x <- rep(1, 1000)
for (i in seq_len(100)) x <- gbm(x, 0.01)
all(x > 0)

}
\seealso{
Other processes: 
\code{\link{process_bessel}()},
\code{\link{process_bm}()},
\code{\link{process_bm_drift_poisson}()},
\code{\link{process_cauchy}()},
\code{\link{process_cir}()},
\code{\link{process_negative_gamma}()},
\code{\link{process_ou}()},
\code{\link{process_wright_fisher}()}
}
\concept{processes}
