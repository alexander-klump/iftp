% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qtps_simulate.R
\name{qtps_simulate}
\alias{qtps_simulate}
\title{Simulate quantile thinning particle system (QTPS)}
\usage{
qtps_simulate(
  n_particles,
  cdf,
  qf = NULL,
  time_horizon,
  process = process_bm(),
  cost = abs,
  init = function(n) rep(0, n),
  method = c("quantiles", "order_statistics"),
  path_resolution = NULL,
  capture_seed = FALSE,
  seed = NULL
)
}
\arguments{
\item{n_particles}{Number of initial particles (at least 2).}

\item{cdf}{Target CDF function or an \code{iftp_dist} object (created by
\code{dist_exponential()} etc.).}

\item{qf}{Quantile function of the target distribution. If \code{cdf} is an
\code{iftp_dist} object with a quantile function, it is used automatically.}

\item{time_horizon}{Time horizon (positive number).}

\item{process}{Transition kernel created by \code{\link[=process_bm]{process_bm()}} or a custom
function with signature \verb{function(x, dt)} that maps current
positions to new positions. Defaults to standard Brownian motion.}

\item{cost}{Cost function applied to particle positions to determine which
particle is removed at each step. Equivalent to the negative fitness
function \code{-F} from Klump (2022, Section 2.4.1). Must be a function
mapping a numeric vector to a numeric vector of the same length.
Defaults to \code{abs} (two-sided boundary).}

\item{init}{Initial distribution function. A function taking a single
argument \code{n} and returning \code{n} starting positions. Defaults to
\code{function(n) rep(0, n)} (all particles at the origin).}

\item{method}{Grid construction method: \code{"quantiles"} (default) builds the
time grid deterministically from the quantile function;
\code{"order_statistics"} samples from the distribution and uses sorted values
as the grid. The latter requires an \code{iftp_dist} object with a sampling
function.}

\item{path_resolution}{Optional positive number. When supplied, each
inter-event interval is sub-divided into steps of at most
\code{path_resolution} time units, producing a finer output time grid.
\code{is_event} marks only the original deletion steps. Defaults to \code{NULL}
(no sub-stepping).}

\item{capture_seed}{Logical. If \code{TRUE} and \code{seed} is \code{NULL}, a random
seed is generated, set, and stored in \code{result$params$seed} for
post-hoc reproducibility. Defaults to \code{FALSE}.}

\item{seed}{Optional integer seed for reproducibility. When provided,
it is always stored in \code{result$params$seed}.}
}
\value{
A \code{ps_result} object (S3 class) containing:
\describe{
\item{type}{Character: \code{"qtps"}.}
\item{times}{Numeric vector of recorded time points (starting at 0).}
\item{is_event}{Logical vector. \code{TRUE} at deletion steps; \code{FALSE} at
time 0 and at sub-steps inserted by \code{path_resolution}.}
\item{positions}{Matrix of size \verb{(n_rows x n_particles)} with particle
positions at each time point; dead particles are \code{NA}.}
\item{params}{List of simulation parameters: \code{n_particles}, \code{time_horizon},
\code{method}, \code{path_resolution}, \code{process}, \code{init}, and \code{cost}.}
}
}
\description{
Simulates a particle system where N particles perform independent
Brownian motions on a quantile-derived time grid. At each step, the
particle with the largest absolute value is permanently removed
(no replacement), so the particle count decreases from N to 1.
}
\details{
The time grid is \code{t_k = qf(k/N)} for \verb{k = 1, ..., floor(cdf(time_horizon)*N)}.
At each step, all surviving particles receive independent increments
from the stochastic process, and the particle with the largest cost(X)
is permanently removed.

Internally, a fixed-length alive-mask is used so that particle indices
remain stable across steps, enabling the full position matrix.
}
\examples{
result <- qtps_simulate(
  n_particles = 100, cdf = dist_exponential(), time_horizon = 2, seed = 42
)
head(result$positions)

}
\references{
Klump, A. (2023). The inverse first passage time problem as hydrodynamic
limit of a particle system. \emph{Methodology and Computing in Applied
Probability}, 25, 42. \doi{10.1007/s11009-023-10020-7}
}
\seealso{
\code{\link[=plot_ps_result]{plot_ps_result()}} for visualization

Other nbmp: 
\code{\link{nbmp_simulate}()}
}
\concept{nbmp}
