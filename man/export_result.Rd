% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps_export.R
\name{export_result}
\alias{export_result}
\title{Export simulation result}
\usage{
export_result(result, file = NULL, format = NULL)
}
\arguments{
\item{result}{An \code{iftp_result} or \code{ps_result} object.}

\item{file}{Output file path. Use \code{.csv} or \code{.json} extension to select
file type. If \code{NULL} (default), returns the data without writing to
disk.}

\item{format}{JSON format for \code{ps_result} objects: \code{"matrix"} or
\code{"segments"}. Required when writing \code{ps_result} to JSON. Ignored for
\code{iftp_result} and for CSV output.}
}
\value{
A data frame or list (invisibly if \code{file} is specified).
}
\description{
Exports a simulation result to CSV, JSON, or returns it as an R object.
}
\details{
For \code{iftp_result}, always returns the boundary data frame (columns:
\code{time}, \code{boundary}).

For \code{ps_result}, the output depends on the file type and \code{format}:
\itemize{
\item \strong{CSV} (or \code{file = NULL} without \code{format}): flat data frame with
columns \code{particle_id}, \code{time}, \code{position}, \code{is_alive}. One row per
(segment, time point) pair, including rows where the particle is dead
(\code{is_alive = FALSE}, \code{position} is \code{NA}).
\item \strong{JSON with \code{format = "matrix"}}: list with elements \code{times} (numeric
vector) and \code{positions} (list of numeric vectors, one per time point).
Direct serialization of the positions matrix; \code{NA} values become \code{null}.
\item \strong{JSON with \code{format = "segments"}}: list of per-segment objects, each
with \code{id} (integer), \code{times} (numeric vector), and \code{positions} (numeric
vector) containing only the non-\code{NA} entries for that segment column.
}

For N-BMP \code{ps_result}, the position matrix columns grow with each event
(branching) and each jump-induced segment break. Each column represents
a contiguous path segment between discontinuities.

For QTPS \code{ps_result}, the position matrix has exactly \code{n_particles}
columns. Deleted particles become \code{NA} from the deletion step onward.

JSON export requires the \pkg{jsonlite} package.
}
\examples{
# IFTP result
b <- iftp_anulova(dist_exponential(),
  time_horizon = 1, time_steps = 10,
  n_particles = 100, seed = 1
)
head(export_result(b))

# Particle system — CSV / data frame (default)
result <- nbmp_simulate(n_particles = 10, time_horizon = 0.5, seed = 42)
head(export_result(result))

# Particle system — matrix format
mat <- export_result(result, format = "matrix")
str(mat) # list with "times" and "positions"

# Particle system — segments format
segs <- export_result(result, format = "segments")
str(segs[[1]]) # list with "id", "times", "positions"

}
\seealso{
Other result-io: 
\code{\link{load_result}()},
\code{\link{save_result}()},
\code{\link{suggest_filename}()}
}
\concept{result-io}
