% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iftp_quantile.R
\name{iftp_quantile}
\alias{iftp_quantile}
\title{Quantile discretization for the IFTP}
\usage{
iftp_quantile(
  cdf,
  qf = NULL,
  time_horizon,
  time_steps,
  remove_per_step,
  process = process_bm(),
  cost = abs,
  init = function(n) rep(0, n),
  method = c("quantiles", "order_statistics"),
  capture_seed = FALSE,
  seed = NULL
)
}
\arguments{
\item{cdf}{Target CDF function or an \code{iftp_dist} object (created by
\code{dist_exponential()} etc.).}

\item{qf}{Quantile function of the target distribution. If \code{cdf} is an
\code{iftp_dist} object with a quantile function, it is used automatically.}

\item{time_horizon}{Time horizon (positive number).}

\item{time_steps}{Refinement parameter. The quantile function is evaluated at
multiples of \code{1/time_steps}.}

\item{remove_per_step}{Number of particles removed per step. The total sample
size is \code{time_steps * remove_per_step}.}

\item{process}{Transition kernel created by \code{\link[=process_bm]{process_bm()}} or a custom
function with signature \verb{function(x, dt)} that maps current
positions to new positions. Defaults to standard Brownian motion.}

\item{cost}{Cost function applied to particle positions before computing
quantiles. Must be a vectorised numeric-to-numeric function.
Equivalent to the negative fitness function \code{-F} from
Klump (2022, Section 2.4.1). Use \code{abs} (default) for a two-sided
(symmetric) boundary, or \code{identity} for a one-sided (upper) boundary.}

\item{init}{Initial distribution function. A function taking a single
argument \code{n} and returning \code{n} starting positions. Defaults to
\code{function(n) rep(0, n)} (all particles at the origin).}

\item{method}{Grid construction method: \code{"quantiles"} (default) builds the
time grid deterministically from the quantile function;
\code{"order_statistics"} samples from the distribution and uses sorted values
as the grid. The latter requires an \code{iftp_dist} object with a sampling
function.}

\item{capture_seed}{Logical. If \code{TRUE} and \code{seed} is \code{NULL}, a random
seed is generated, set, and stored in \code{result$params$seed} for
post-hoc reproducibility. Defaults to \code{FALSE}.}

\item{seed}{Optional integer seed for reproducibility. When provided,
it is always stored in \code{result$params$seed}.}
}
\value{
An \code{iftp_result} object (a list with elements \code{boundary} and
\code{params}). \code{boundary} is a data frame with columns:
\describe{
\item{time}{Non-equidistant time points derived from the quantile
function.}
\item{boundary}{Particle positions at the survival threshold.
The survival region is \code{\{(t, x) : cost(x) <= cost(b(t))\}}.}
}
}
\description{
Approximates the boundary of the inverse first-passage time problem using
a non-equidistant time grid derived from the target distribution's quantile
function. A fixed number of particles are removed at each step.
}
\details{
Unlike the Anulova discretization, which uses an equidistant time grid,
this method computes the time grid from the quantile function of the target
distribution. At each step, exactly \code{remove_per_step} particles are removed
(those with the largest values of \code{cost(X)}), giving a fixed reduction per
step.

The time grid is \code{t_k = qf(k/time_steps)} for
\verb{k = 1, ..., floor(cdf(time_horizon)*time_steps)}, where
\code{qf} is the quantile function of the target distribution.

The boundary at each step is the \code{(n_alive - remove_per_step) / n_alive} quantile
of \code{cost(X)} among surviving particles, and particles with
\code{cost(X) > boundary} are removed.
}
\examples{
d <- dist_exponential(rate = 1)
result <- iftp_quantile(
  cdf = d, time_horizon = 2, time_steps = 50,
  remove_per_step = 20, seed = 42
)
head(result$boundary)

}
\references{
Klump, A. (2022). \emph{The classical and the soft-killing Inverse First-Passage
Time Problem: a stochastic order approach}. PhD thesis, University of Paderborn. Section 2.4.3.
}
\seealso{
Other iftp: 
\code{\link{iftp_anulova}()},
\code{\link{iftp_soft_killing}()},
\code{\link{plot_iftp_result}()}
}
\concept{iftp}
