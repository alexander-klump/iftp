apiVersion: v1
kind: Service
metadata:
  name: {{ include "iftp.fullname" . }}
  labels:
    {{- include "iftp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iftp.selectorLabels" . | nindent 4 }}
